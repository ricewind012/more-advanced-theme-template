{
  "type": "module",
  "scripts": {
    "build": "node get_steam_version && npx postcss --verbose 'src/**/*.scss' --base src --dir dist --ext css"
  },
  "dependencies": {
    "@csstools/postcss-sass": "^5.1.1",
    "postcss-cli": "^11.0.0",
    "postcss-functions": "^4.0.2",
    "steam-theming-utils": "^3.1.2"
  }
}
